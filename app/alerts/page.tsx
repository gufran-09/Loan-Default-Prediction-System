'use client'
import {useEffect,useState} from 'react';import Link from 'next/link';import {Shell} from '@/components/dashboard/shell';import {Bell,ArrowUpRight} from 'lucide-react'
export default function Alerts(){const [alerts,setAlerts]=useState<any[]>([]);useEffect(()=>{fetch('/api/alerts').then(r=>r.json()).then(x=>setAlerts(x.data||[]))},[]);return <Shell><div className="flex flex-col gap-8"><div><p className="font-mono text-xs uppercase tracking-[0.2em] text-muted-foreground">Monitoring</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Alerts</h1><p className="mt-2 text-sm text-muted-foreground">Signals that need a risk officer&apos;s attention.</p></div><div className="flex flex-col gap-3">{alerts.map(a=><article key={a.id} className="flex flex-col gap-4 rounded-xl border bg-card p-5 md:flex-row md:items-center md:justify-between"><div className="flex gap-4"><div className="mt-0.5 flex size-9 shrink-0 items-center justify-center rounded-lg bg-destructive/10 text-destructive"><Bell className="size-4"/></div><div><div className="flex flex-wrap items-center gap-2"><h2 className="font-medium">{a.title}</h2><span className="rounded-full bg-secondary px-2 py-0.5 text-xs capitalize">{a.severity}</span></div><p className="mt-1 text-sm leading-6 text-muted-foreground">{a.description}</p><p className="mt-2 text-xs text-muted-foreground">{a.borrowers?.full_name} · {new Date(a.created_at).toLocaleDateString()}</p></div></div><Link href={`/borrowers/${a.borrower_id}`} className="flex items-center gap-1 text-sm font-medium text-primary">Review<ArrowUpRight className="size-4"/></Link></article>)}</div></div></Shell>}
