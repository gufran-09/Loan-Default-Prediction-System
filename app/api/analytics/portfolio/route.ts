import { NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'
export async function GET(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)return NextResponse.json({error:{code:'UNAUTHORIZED',message:'Authentication required'}},{status:401});const {data,error}=await supabase.from('borrowers').select('loan_type,geography,tenure_months,risk_scores(bucket,score)');if(error)return NextResponse.json({error:{code:'DB_ERROR',message:'Unable to load analytics'}},{status:500});const rows=data||[];const group=(key:string)=>Object.values(rows.reduce((a:any,r:any)=>{const k=r[key];a[k]??={name:k,total:0,score:0};a[k].total++;a[k].score+=Number(r.risk_scores?.score||0);return a},{})).map((x:any)=>({...x,score:Number((x.score/x.total).toFixed(1))}));return NextResponse.json({data:{byLoanType:group('loan_type'),byGeography:group('geography'),byTenure:group('tenure_months')}})}
