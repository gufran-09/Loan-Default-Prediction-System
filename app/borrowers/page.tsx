'use client'
import Link from 'next/link';import {useEffect,useState} from 'react';import {Shell} from '@/components/dashboard/shell';import {Search,ArrowUpRight} from 'lucide-react'
export default function Borrowers(){const [rows,setRows]=useState<any[]>([]);const [search,setSearch]=useState('');useEffect(()=>{fetch(`/api/borrowers?pageSize=50&search=${encodeURIComponent(search)}`).then(r=>r.json()).then(x=>setRows(x.data||[]))},[search]);return <Shell><div className="flex flex-col gap-8"><div><p className="font-mono text-xs uppercase tracking-[0.2em] text-muted-foreground">Portfolio</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Borrowers</h1><p className="mt-2 text-sm text-muted-foreground">Search and triage the monitored borrower book.</p></div><div className="flex h-11 max-w-md items-center gap-3 rounded-lg border bg-card px-3"><Search className="size-4 text-muted-foreground"/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search name or borrower ID" className="w-full bg-transparent text-sm outline-none"/></div><div className="overflow-hidden rounded-xl border bg-card"><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-muted/40 text-xs uppercase tracking-wide text-muted-foreground"><tr><th className="px-5 py-3 font-medium">Borrower</th><th className="px-5 py-3 font-medium">Loan</th><th className="px-5 py-3 font-medium">Balance</th><th className="px-5 py-3 font-medium">Risk</th><th/></tr></thead><tbody className="divide-y">{rows.map(r=>{const s=r.risk_scores?.[0];return <tr key={r.id}><td className="px-5 py-4"><Link className="font-medium hover:underline" href={`/borrowers/${r.id}`}>{r.full_name}</Link><p className="font-mono text-xs text-muted-foreground">{r.external_id}</p></td><td className="px-5 py-4 text-muted-foreground">{r.loan_type}</td><td className="px-5 py-4">${Number(r.outstanding_balance).toLocaleString()}</td><td className="px-5 py-4"><span className="rounded-full bg-secondary px-2.5 py-1 text-xs font-medium">{s?.bucket} · {s?.score}</span></td><td className="px-5 py-4 text-right"><Link href={`/borrowers/${r.id}`}><ArrowUpRight className="ml-auto size-4 text-muted-foreground"/></Link></td></tr>})}</tbody></table></div></div></div></Shell>}
